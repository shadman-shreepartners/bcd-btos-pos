export type TripBooking = { id: string | number };

export type TripMeta = { total?: number; page?: number; perPage?: number; lastPage?: number };

export type TripLibraryFilters = Record<string, never>;
